eureka:
    client:
        serviceUrl:
            defaultZone: http://registry:9988/eureka
server:
    port: 8788
spring:
    application:
        name: channel-service
    datasource:
        driver-class-name: org.postgresql.Driver
        password: root123
        url: jdbc:postgresql://postgres1:5432/channeldb
        username: mirazul
    jpa:
        hibernate:
            ddl-auto: create
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQL95Dialect
        show-sql: false
